import React from 'react';
import { Question, QuestionType } from '../../../../api';

interface Props {
    question: Question<QuestionType.TextGapsVariant>;
}

export const TextGapsVariant: React.FC<Props> = ({ question }) => {

    return <div></div>
}